#include <iostream>
#include <stdio.h>

using namespace std;

int main()
{
    //freopen("entrada.txt", "r", stdin);
    //freopen("entrada.txt", "w", stdout);
    char a;

    while( scanf("%c", &a)!= EOF){
        printf("%c", a);
    }

    return 0;
}
